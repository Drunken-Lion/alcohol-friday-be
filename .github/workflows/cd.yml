name: cd

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: create application.yml
        run: |
          touch ./application.yml
          echo '${{ secrets.DEV_APPLICATION_YML }}' >> ./application.yml

      - name: create application-file.yml
        run: |
          touch ./application-file.yml
          echo '${{ secrets.DEV_APPLICATION_FILE_YML }}' >> ./application-file.yml

      - name: create application-secret.yml
        run: |
          touch ./application-secret.yml
          echo '${{ secrets.DEV_APPLICATION_SECRET_YML }}' >> ./application-secret.yml

      - name: create application-test.yml
        run: |
          touch ./application-dev.yml
          echo '${{ secrets.DEV_APPLICATION_DEV_YML }}' >> ./application-dev.yml

      - name: Login ECR
        run: |
          docker login -u ${{ secrets.NCP_ACCESS_KEY }} -p ${{ secrets.NCP_SECRET_KEY }} ${{ secrets.NCP_ECR_URL }}

      - name: Make Docker Image
        run: |
          docker build --build-arg PROFILE=dev -t alcohol-friday:${GITHUB_SHA::8} .

      - name: Tag Docker Image
        run: |
          docker tag alcohol-friday:${GITHUB_SHA::8} ${{ secrets.NCP_ECR_URL }}/alcohol-friday:${GITHUB_SHA::8}

      - name: Docker Image Push To ECR
        run: |
          docker push ${{ secrets.NCP_ECR_URL }}/alcohol-friday:${GITHUB_SHA::8}

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: NCP Login And Deploy Script Run
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.NCP_HOST }}
          username: ${{ secrets.NCP_USERNAME }}
          password: ${{ secrets.NCP_PASSWORD }}
          port: ${{ secrets.NCP_PORT }}
          script: |
            cd /usr/local/docker
            ./deploy-server.sh "${{ secrets.NCP_ACCESS_KEY }}" "${{ secrets.NCP_SECRET_KEY }}" "${{ secrets.NCP_ECR_URL }}" ${GITHUB_SHA::8} "dev"
