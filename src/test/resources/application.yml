spring:
  profiles:
    active: test
    group:
      local:
        - file
      test:
        - file
        - secret
logging:
  level:
    com.drunkenlion.alcoholfriday: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.orm.jdbc.extract: TRACE
    org.springframework.transaction.interceptor: TRACE
