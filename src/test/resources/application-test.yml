spring:
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:h2:mem:demo_test;
  jpa:
    properties:
      hibernate:
        default_batch_fetch_size: 100
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
    hibernate:
      ddl-auto: create-drop